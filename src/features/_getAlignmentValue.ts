// type HasAlignment = ComponentNode | ComponentSetNode | FrameNode | InstanceNode;
//
// export function getAlignmentValue(node: HasAlignment): string {
//   const prop = node.primaryAxisAlignItems;
//   return prop === 'SPACE_BETWEEN' ? 'MIN' : prop;
// }
